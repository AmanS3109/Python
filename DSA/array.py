array = ['a', 'b', 'c', 'd']
# 4*4 = 16 bits

# Push append(value we want to put, default push in the last position)
array.append('e')

# insert(index, value)
array.insert(5, 'f')

# pop(index)
array.pop(5)
print(array)
